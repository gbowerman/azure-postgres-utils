# Postgres on Azure CLI scripts

[create-replica.sh](create-replica.sh) - Script to deploy Azure Database for PostgreSQL server, create a read replica in another region with a virtual endpoint.  

[deploy-pgfs-pe.sh](deploy-pgfs-pe.sh) - Script to deploy Azure Database for PostgreSQL server in a secure network configuration with private endpoints.  
